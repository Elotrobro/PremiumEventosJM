from django.db import models

# La app `core` es solo de "front" (páginas estáticas + estáticos), por
# eso no define ningún modelo. Todos los modelos del proyecto viven en
# Bd_PremiumEventos/models.py (Usuario, Cliente, Cotizacion, etc.).
