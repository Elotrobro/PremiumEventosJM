from django.contrib import admin

# La app `core` no define ningún modelo propio (ver core/models.py),
# por lo que no hay nada que registrar aquí. Todos los modelos del
# proyecto (Usuario, Cliente, Cotizacion, etc.) están registrados en
# Bd_PremiumEventos/admin.py.
